batchprocessHDL.sh is a shell script that runs hdl_dump inject_dvd for each iso file in the same directory as itself

run the script, provide the device (/dev/<drive>), then provide the name you want to see in OPL for each ISO

This script uses hdl-dump compiled for Linux (https://github.com/ps2homebrew/hdl-dump)
